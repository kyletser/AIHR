# OfferCatcher — 学生求职匹配智能体 方案说明

## 一、问题诊断

学生在求职时面临三个核心痛点：

1. **方向迷茫**：不确定自己的技能和经历适合哪些岗位，缺乏定位
2. **匹配盲投**：选好目标岗位后，不知道简历与 JD 匹配度如何、该从哪改起
3. **反馈缺失**：投递后石沉大海，没有数据反馈来校准下一步策略

本质上是信息不对称 + 反馈链路断裂——学生不了解 JD 背后真正的技能权重，也不清楚自己的简历中"加分项"和"无效信息"分别是什么，更无法从投递结果中学习。

## 二、方案设计

### 整体架构：四层智能体

```
┌────────────────────────────────────────────────┐
│  ① 目标规划层 — 基于简历，推荐最匹配的职业方向    │
│  ② 匹配分析层 — JD基因解码 → 代码验证 → 确定性评分│
│  ③ 优化建议层 — 简历改写 + 差距分析 + 投递策略    │
│  ④ 反馈闭环层 — 投递记录 → 数据校准 → 智能追问    │
└────────────────────────────────────────────────┘
```

### ② 匹配分析层（核心引擎）

采用 **四步分离架构**，解决 LLM 幻觉和评分不一致问题：

1. **LLM 提取**：从简历和 JD 中提取结构化数据（技能、项目、竞赛等）
2. **代码验证**：每个提取项必须在原文中找到证据，找不到直接剔除
3. **代码评分**：纯 Python 计算 6 维分数（同义词匹配 + 学历映射 + 竞赛加分），结果完全确定性
4. **LLM 文案**：基于已验证数据生成可读的分析描述、差距分析和优化建议

### 6 维评分体系

| 维度 | 权重 | 核心逻辑 |
|------|------|---------|
| 硬技能 | 35% | 70+ 技能同义词 + 可迁移技能检测 |
| 项目经历 | 25% | 项目技术与 JD 关键词重叠度 |
| 学历 | 15% | 学位与岗位要求对比 |
| 软素质 | 15% | 11 类软素质信号（领导力/沟通/自驱力等）|
| 行业认知 | 5% | 13 个职业领域匹配 |
| 成长潜力 | 5% | 竞赛获奖 + 技能广度 + 证书 |

### ③ 投递策略引擎

| 总分 | 策略 | 动作 |
|------|------|------|
| ≥82 | 优先投递 | 重点打磨简历表达 |
| ≥68 | 优化后投递 | 补强 1-2 个证据点 |
| ≥55 | 谨慎投递 | 冲刺用，先补短板 |
| <55 | 暂缓投递 | 换更贴合岗位 |

超出单纯分数：还输出**风险雷达**（指出高/中风险维度）、**学习冲刺计划**（补短路径+时间预估）、**简历聚焦建议**。

### ① + ④ 求职智能体对话

- **目标规划**：内置 9 个行业模板，根据简历关键词自动推荐 5 个最适合的职业方向
- **智能追问**：自动识别简历信息缺口（学历/经历/量化/证书/技能/软素质），生成引导性追问
- **投递反馈校准**：记录投递状态（面试/录用/被拒），反向调整推荐分数阈值和薄弱维度
- **AI 对话顾问**：基于当前简历、JD 和匹配结果，回答学生关于求职方向、简历优化、投递策略的问题

## 三、AI 工具选型

| 组件 | 选型 | 理由 |
|------|------|------|
| LLM | 智谱 GLM-4.5-Air | 多行业语义理解能力强，API 稳定，中文优化 |
| 评分 | 纯代码（Python） | LLM 不适合做确定性计算，代码评分零幻觉 |
| 验证 | 纯代码（正则+子串匹配） | 无需 LLM，快速精确 |
| 前端 | Vue 3 + Vite + TailwindCSS | 轻量 SPA，开发效率高 |
| 后端 | FastAPI + SQLAlchemy | 异步高性能，自动生成 API 文档 |
| 数据库 | SQLite | 零配置，单文件部署，适合 MVP |

**核心设计原则**：LLM 只用于"不可替代的自然语言环节"（提取技能、写文案），评分和验证完全用代码实现。

## 四、关键配置

- `LLM_PROVIDER=zhipu`（可切换 DeepSeek/SiliconFlow）
- `ZHIPU_MODEL=glm-4.5-air`
- 数据库：SQLite（DATABASE_URL=sqlite:///./offer_catcher.db）
- 部署：FastAPI 内嵌 SPA 静态文件，单端口 8088
- 端口 80：StudyBuddy 个人网站，端口 8088：OfferCatcher

## 五、迭代记录

| 阶段 | 关键改进 |
|------|---------|
| V1 | 单次 LLM 调用完成分析（幻觉严重，分数波动大）|
| V2 | 引入代码验证 + 确定性评分，架构重构为 4 步流水线 |
| V2.1 | 扩展 13 个职业领域 + 可迁移技能 + 11 类软素质识别 |
| V2.2 | JWT 多用户 + 分析历史持久化 + 简历版本管理 |
| V2.3 | 投递策略引擎：风险雷达 + 学习冲刺 + 简历聚焦 |
| V2.4 | 求职智能体：目标规划 + 追问 + AI 对话 + 反馈校准 |

## 六、效果评估

- **评分一致性**：同一简历+JD 重复 10 次，分数波动为 0（确定性代码）
- **幻觉控制**：验证器剔除率 5-15%，有效过滤 LLM 编造内容
- **响应速度**：完整分析 15-30 秒（2 次 LLM 调用 + 代码计算）
- **行业覆盖**：12 个预设岗位模板，覆盖 8 个行业
- **公网部署**：http://150.158.89.70:8088，管理员 admin/admin
